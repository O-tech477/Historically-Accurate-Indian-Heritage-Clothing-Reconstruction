import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.otech.clothreconstruction',
  appName: 'ClothReconstruction',
  webDir: 'build'
};

export default config;
